# 2026-05-10 — Weekly Status Snapshot

## Summary
Quiet week on the `novacifer.com` repo: zero new commits since `d1d47dd` (2026-05-03). Site is live, deploy pipeline green, no incidents. This entry is a tracking checkpoint, not a changelog.

## Active projects — current status

### Social Media Reviews
- Status: launching
- Channels: LinkedIn, Twitter/X (@emerjux)
- Cadence: 1–2 posts/week
- This week: no new dispatch landed in the build-log from this repo's perspective. Verify in `jmolinasoler/build-log` whether W19 post shipped.

### Crypto Compliance Stack — MiCAMap
- Status: in development
- Live artefact: https://micadash.novacifer.com
- Pending check: regulator delta feed, source-of-truth list, alerting hook design.

### Agentic AI & Observability Lab
- Status: active
- Stack: self-hosted Langfuse + OpenRouter + Node.js
- Note: site-side Langfuse client tracing was removed on 2026-05-03 — observability remains backend-only, as intended.

## Repo backlog (carried over)
- `assets/images/og-image.png` (1200×630) for Twitter/LinkedIn cards — `og:image` still points at `favicon.svg`.
- `git rm -r .github-workflows-pending/` — directory still present locally; safe to delete.
- Verify `prefers-reduced-motion` actually disables gear/typewriter animations in `assets/js/main.js`.
- Nav active-state edge case (trailing slash) — re-test after next Jekyll bump.

## Next week — intent
- Ship one Social Media Review post (Hetzner CCX vs Scaleway DEV1 pricing/perf or PostgreSQL 17 vs SurrealDB 2.x for write-heavy workloads — pick one).
- Land `og-image.png` and update `_layouts/default.html` meta tags.
- Trim `.github-workflows-pending/` from the tree.

## Verification
- `git log --since=2026-05-03` → 0 new commits prior to this entry.
- `git status` → clean working tree (only untracked `.claude/` settings dir).
- Site reachable at https://www.novacifer.com (HTTP 200 expected — not re-verified this run).
